X means 'fiXed'

[X] The notification shows how much time is left
[ ] No negative time. Create a keyboard (sorta) for positive numbers only
[ ] Block user when tries to do two task at one time
[ ] Progress bar only shows on some devices (and browsers)
[X] Time lags when in background
[ ] Remove and Edit tasks
[ ] Change the last day (Sunday by deafult)
[ ] Make the app to work with local notifications
[ ] Set tasks more important than others.
[ ] Make the app more pretty
